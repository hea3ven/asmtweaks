1.1.1:
	* Add the ability to have client side only asm mods.
	* Update to java 8.

1.1.0:
	* Fix running in deobfuscated environments when no mapping is available.
	* Add the ability to use forge deobfuscation data if found.
	* Add MethodEditor that simplifies the editing of methods.

1.0.2:
	* Fix the tweak manager creating a class mapping for every loaded class, fixes #1.

1.0.1:
	* Fix errors when running in deobfuscated environment.

1.0.0:
	* Initial release
